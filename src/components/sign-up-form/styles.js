import { styled } from "styled-components";
import { Field } from 'formik';

export const FormField = styled(Field)`
  display: block;
  padding: 10px 5px;
  margin: 10px auto;
  width: 80%;
  overflow: hidden;
  font-size: 14px;
`;